var user = exports;

//list all active users (probably shouldn't expose this)
user.list = function(request, response) {
  
};

user.save = function(request, response) {
  
};

user.update = function(request, response) {
  
};